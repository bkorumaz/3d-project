import styled from 'styled-components'

const Wrapper = styled.div`
    position: relative;
    background: #1f1144

    canvas {
        height: 500px;
        background: red;
    }
`

export default Wrapper;
